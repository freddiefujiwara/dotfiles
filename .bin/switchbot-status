#!/bin/bash
SCRIPT_DIR=$(cd "$(dirname "$0")" && pwd)
. "$SCRIPT_DIR/switchbot-common"

while getopts d: option; do
  case "${option}" in
    d) deviceid=${OPTARG} ;;
  esac
done
[[ -z ${deviceid:-} ]] && { echo "Usage $0 -d <deviceid>"; exit 1; }
switchbot_get "/devices/${deviceid}/status" | jq
